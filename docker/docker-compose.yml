services:
  app:
    build:
      context: ..
      dockerfile: docker/Dockerfile
      target: prod
    ports:
      - "3000:3000"
  firebase:
    build:
      context: ..
      dockerfile: dockerfile
      target: prod
    ports:
      - "1231:1231"